{{ render(
    controller(
        'ng_content:viewAction', {
            'content': item.object.content,
            'location': item.object,
            'viewType': view_type,
            'layout': false,
            'params': {
                'nglayouts_item': item,
            }|merge(ezparams|default({}))
        }
    )
) }}
