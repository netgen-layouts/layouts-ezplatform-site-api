{{ render(
    controller(
        'ng_content:viewAction', {
            'content': item.object.content,
            'location': item.object,
            'viewType': 'ngbm_app_preview',
            'layout': false,
            'params': {
                'ngbm_item': item,
            }
        }
    )
) }}
