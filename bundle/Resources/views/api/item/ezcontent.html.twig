{{ render(
    controller(
        'ng_content:viewAction', {
            'content': item.object,
            'location': item.object.mainLocation,
            'viewType': 'nglayouts_app_preview',
            'layout': false,
            'params': {
                'nglayouts_item': item,
            }
        }
    )
) }}
